spring:
  application:
    name: backend
  datasource:
    url: jdbc:postgresql://localhost:5432/order_system
    username: orderapp
    password: orderapp123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

order:
  executor:
    pool-size: 8
  retry:
    max-retries: 3
